meta {
  name: Create Category
  type: http
  seq: 1
}

post {
  url: {{base_url}}/equipment-categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

body:json {
  {
    "name": "เครื่องเสียง"
  }
}
