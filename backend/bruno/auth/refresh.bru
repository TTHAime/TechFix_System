meta {
  name: Refresh Token
  type: http
  seq: 8
}

post {
  url: {{base_url}}/auth/refresh
  body: none
  auth: none
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("access_token", res.body.accessToken);
  }
}
