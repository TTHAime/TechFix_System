meta {
  name: Login - Wrong Password
  type: http
  seq: 6
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{admin_email}}",
    "password": "wrongpassword"
  }
}
