meta {
  name: Update My Profile - Weak Password (400)
  type: http
  seq: 16
}

patch {
  url: {{base_url}}/users/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{user_token}}
}

body:json {
  {
    "password": "123"
  }
}
