meta {
  name: SQL Injection by path parameter
  type: http
  seq: 3
}

get {
  url: {{base_url}}/users/1 OR 1=1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
