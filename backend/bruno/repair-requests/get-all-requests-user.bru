meta {
  name: Get All Requests (User - own only)
  type: http
  seq: 7
}

get {
  url: {{base_url}}/repair-requests
  body: none
  auth: bearer
}

auth:bearer {
  token: {{user_token}}
}

params:query {
  ~page: 1
  ~limit: 20
}
