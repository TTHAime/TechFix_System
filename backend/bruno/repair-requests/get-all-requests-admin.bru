meta {
  name: Get All Requests (Admin)
  type: http
  seq: 6
}

get {
  url: {{base_url}}/repair-requests
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

params:query {
  ~page: 1
  ~limit: 20
}
