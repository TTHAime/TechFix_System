meta {
  name: Resolve Item (Technician) - via resolve endpoint
  type: http
  seq: 31
}

patch {
  url: {{base_url}}/repair-requests/2/items/1/resolve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tech_token}}
}

body:json {
  {
    "partsUsed": "RAM 8GB",
    "repairSummary": "เพิ่ม RAM แก้ปัญหาค้าง"
  }
}
