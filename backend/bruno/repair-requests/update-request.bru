meta {
  name: Update Request (change status)
  type: http
  seq: 11
}

patch {
  url: {{base_url}}/repair-requests/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

body:json {
  {
    "statusId": 2
  }
}
