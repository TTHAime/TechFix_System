meta {
  name: Update Department - Duplicate (409)
  type: http
  seq: 8
}

patch {
  url: {{base_url}}/departments/6
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

body:json {
  {
    "name": "ฝ่ายเทคโนโลยีสารสนเทศ"
  }
}
