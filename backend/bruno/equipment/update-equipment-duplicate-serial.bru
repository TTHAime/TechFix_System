meta {
  name: Update Equipment - Duplicate Serial (409)
  type: http
  seq: 9
}

patch {
  url: {{base_url}}/equipment/6
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

body:json {
  {
    "serialNo": "PC-ACC-001"
  }
}
