headers {
  Content-Type: application/json
  Origin: http://localhost:5173
}

auth {
  mode: bearer
}

auth:bearer {
  token: {{access_token}}
}

script:pre-request {
  // If no token yet, skip (auth requests handle their own login)
}
