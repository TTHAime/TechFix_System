meta {
  name: Update Role
  type: http
  seq: 8
}

patch {
  url: {{base_url}}/roles/5
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

body:json {
  {
    "name": "supervisor"
  }
}
