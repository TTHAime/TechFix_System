meta {
  name: Update Role - Duplicate Name (409)
  type: http
  seq: 9
}

patch {
  url: {{base_url}}/roles/5
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

body:json {
  {
    "name": "admin"
  }
}
