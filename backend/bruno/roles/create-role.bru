meta {
  name: Create Role
  type: http
  seq: 1
}

post {
  url: {{base_url}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

body:json {
  {
    "name": "manager"
  }
}
