meta {
  name: Create Role - User Role (403)
  type: http
  seq: 4
}

post {
  url: {{base_url}}/roles
  body: json
  auth: bearer
}

auth:bearer {
  token: {{user_token}}
}

body:json {
  {
    "name": "test"
  }
}
