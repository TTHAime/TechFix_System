meta {
  name: Get All Roles
  type: http
  seq: 5
}

get {
  url: {{base_url}}/roles
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

params:query {
  ~page: 1
  ~limit: 20
}
